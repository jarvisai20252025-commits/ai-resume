{
  "name": "ai-resume-builder",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python main.py"
  },
  "dependencies": {},
  "devDependencies": {}
}